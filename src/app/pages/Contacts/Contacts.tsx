import React, { FC } from "react";
import Navbar from "../../components/Navbar";

const Contacts: FC = () => {
  return (
    <div>
      <Navbar />
      <h1>Contacts</h1>
    </div>
  );
};

export default Contacts;
